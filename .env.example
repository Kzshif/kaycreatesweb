# Copy to .env.local and fill in.
# Required for the live AI receptionist. Without it, the demo falls back to a
# scripted rule-based receptionist so you can still click through the product.
ANTHROPIC_API_KEY=
